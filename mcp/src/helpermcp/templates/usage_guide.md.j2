{#- Usage Guide Template -#}
# {{ tool.display_name }}

> **Certified**: ✅ Passed {{ tool.certification_attempts }} tests
> **Score**: {{ tool.detailed_score.aggregate }}/10

## Description

{{ tool.description }}

{% if tool.intent %}
**Intent**: {{ tool.intent }}
{% endif %}

## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
{% for param in tool.parameters %}
| `{{ param.name }}` | `{{ param.type_hint }}` | {{ "Yes" if param.required else "No" }} | {{ param.description }} |
{% endfor %}

## Scoring Breakdown

| Dimension | Score | Reason |
|-----------|-------|--------|
| LLM Utility | {{ tool.detailed_score.llm_utility }}/10 | {{ tool.detailed_score.llm_utility_reason }} |
| Determinism | {{ tool.detailed_score.determinism }}/10 | {{ tool.detailed_score.determinism_reason }} |
| Token Efficiency | {{ tool.detailed_score.token_efficiency }}/10 | {{ tool.detailed_score.token_efficiency_reason }} |

## Best Practices

1. **Authentication**: Ensure `{{ auth_env_var }}` is set in your environment
2. **Error Handling**: Always check the `error` field in responses
3. **Rate Limits**: Respect API rate limits (typically 100 req/min)

## Example Usage

```python
# Via MCP client
result = await mcp.call_tool("{{ tool.name }}", {
    {% for param in tool.parameters %}
    "{{ param.name }}": {{ param.examples[0] if param.examples else '""' }},
    {% endfor %}
})
```

## Common Errors

| Error | Cause | Solution |
|-------|-------|----------|
| `401 Unauthorized` | Invalid API key | Check `{{ auth_env_var }}` environment variable |
| `429 Too Many Requests` | Rate limit exceeded | Implement exponential backoff |
| `400 Bad Request` | Invalid parameters | Validate input before calling |

---
*Auto-generated by HelperMCP*
